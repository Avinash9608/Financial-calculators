<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>HRA Calculator</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Poppins", sans-serif;
        background: linear-gradient(135deg, #74ebd5, #acb6e5);
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
      }

      .container {
        background: white;
        padding: 40px;
        border-radius: 15px;
        box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
        width: 100%;
        max-width: 500px;
        animation: fadeIn 1s ease-in-out;
      }

      .container h1 {
        font-size: 2.5rem;
        margin-bottom: 20px;
        text-align: center;
        color: #444;
      }

      .form-group {
        margin-bottom: 20px;
      }

      .form-group label {
        display: block;
        margin-bottom: 5px;
        font-weight: bold;
        color: #555;
      }

      .form-group input,
      .form-group select {
        width: 100%;
        padding: 10px;
        border-radius: 8px;
        border: 1px solid #ddd;
        font-size: 1rem;
        transition: all 0.3s ease;
      }

      .form-group input:focus,
      .form-group select:focus {
        border-color: #74ebd5;
        box-shadow: 0 0 8px rgba(116, 235, 213, 0.5);
        outline: none;
      }

      .btn-success {
        background: linear-gradient(135deg, #43cea2, #185a9d);
        color: white;
        border: none;
        font-size: 1.2rem;
        padding: 10px 20px;
        border-radius: 25px;
        cursor: pointer;
        width: 100%;
        transition: transform 0.3s ease, background 0.3s ease;
      }

      .btn-success:hover {
        transform: translateY(-3px);
        background: linear-gradient(135deg, #185a9d, #43cea2);
      }

      h2 {
        margin-top: 20px;
        text-align: center;
        color: #444;
        animation: popIn 0.5s ease-out;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(-30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @keyframes popIn {
        from {
          opacity: 0;
          transform: scale(0.8);
        }
        to {
          opacity: 1;
          transform: scale(1);
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>HRA Calculator</h1>
      <form action="/hra" method="post">
        <div class="form-group">
          <label for="basicSalary">Basic Salary (₹):</label>
          <input
            type="number"
            name="basicSalary"
            id="basicSalary"
            required
            placeholder="Enter your basic salary"
          />
        </div>
        <div class="form-group">
          <label for="hraReceived">HRA Received (₹):</label>
          <input
            type="number"
            name="hraReceived"
            id="hraReceived"
            required
            placeholder="Enter your HRA received"
          />
        </div>
        <div class="form-group">
          <label for="rentPaid">Rent Paid (₹):</label>
          <input
            type="number"
            name="rentPaid"
            id="rentPaid"
            required
            placeholder="Enter the rent you paid"
          />
        </div>
        <div class="form-group">
          <label for="cityRent">City Rent (₹):</label>
          <input
            type="number"
            name="cityRent"
            id="cityRent"
            required
            placeholder="Enter the city rent"
          />
        </div>
        <div class="form-group">
          <label for="salary">Salary (₹):</label>
          <input
            type="number"
            name="salary"
            id="salary"
            required
            placeholder="Enter your total salary"
          />
        </div>
        <div class="form-group">
          <label for="cityType">City Type:</label>
          <select name="cityType" id="cityType" required>
            <option value="metro">Metro</option>
            <option value="nonMetro">Non-Metro</option>
          </select>
        </div>
        <button type="submit" class="btn btn-success">Calculate HRA</button>
      </form>

      <% if (typeof result !== 'undefined') { %>
      <h2>Your Eligible HRA Exemption: ₹<%= result %></h2>
      <% } %> <% if (typeof error !== 'undefined') { %>
      <div class="alert alert-danger mt-3"><%= error %></div>
      <% } %>
    </div>
  </body>
</html>
